# Base de données Pro Récup

Ce dossier contient les fichiers permettant de recréer la base PostgreSQL du projet.

## Fichiers

```text
schema.sql
donnees-demo.sql
derniere-sauvegarde.txt

## Remarque sur l’export

La base de données est hébergée sur Supabase.

L’export local avec la commande :

```cmd
npm run db:export